/**
 * @file 路由文件
 * @author donghongyan
 * @date 2020-06-01
 */

import Vue from 'vue';
import Router from 'vue-router';
import config from './config';

Vue.use(Router);

export default new Router({
    routes: config
});
